contract Bench_SysParamString {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s string
        while i < $n{
            s = SysParamString("founder_account")
            i = i + 1
        }
    }
}