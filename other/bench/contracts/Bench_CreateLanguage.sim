contract Bench_CreateLanguage {
    data {

  n int  }
    conditions {

    }
    action {
            var i int
            var s string
            while (i < $n) {
                CreateLanguage("lang" + Str(i), "{'en': 'false', 'ru' : 'true'}")
                i = i + 1
        }
    

    }
}