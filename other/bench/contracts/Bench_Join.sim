contract Bench_Join {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s string, myarr array
        myarr[0] = "first"
        myarr[1] = 10
        while i < $n{
            s = Join(myarr, ",")
            i = i + 1
        }
    }
}