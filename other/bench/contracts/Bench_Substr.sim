contract Bench_Substr {
    data {
    n int
    }
    conditions {

    }
    action {
        var i int
        var s string
        while i < $n{
            s = Substr("string", 1, 10)
            i = i + 1
        }
    }
}