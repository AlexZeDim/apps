contract Bench_RowConditions {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s int
        while i < $n{
            s = RowConditions("@1parameters", 1, false)
            i = i + 1
        }
    }
}