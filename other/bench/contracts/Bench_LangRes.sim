contract Bench_LangRes {
    data {
    n int
    }
    conditions {
    }
    action {
            var i int
            var s string
            while (i < $n) {
                s = LangRes("test", "de")
                i = i + 1
        }
    }
}