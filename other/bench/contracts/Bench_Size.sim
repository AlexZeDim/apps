contract Bench_Size {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s int
        while i < $n{
            s = Size(`test`)
            i = i + 1
        }
    }
}