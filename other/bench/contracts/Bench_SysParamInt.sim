contract Bench_SysParamInt {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s string
        while i < $n{
            s = SysParamInt("founder_account")
            i = i + 1
        }
    }
}