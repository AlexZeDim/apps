contract Bench_GetContractByName {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s int
        while i < $n{
            s = GetContractByName("MainCondition")
            i = i + 1
        }
    }
}