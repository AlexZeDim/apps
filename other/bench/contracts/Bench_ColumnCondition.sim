contract Bench_ColumnCondition {
    data {

  n int  }
    conditions {

    }
    action {
            var i int
            var permissions map
            permissions["update"] = "true"
            permissions["read"] = "true"
            var perms string
            perms = JSONEncode(permissions)
            while (i < $n) {
                ColumnCondition("test", "test" + Str(i), "varchar", perms)
                i = i + 1
        }
    
    }
}