contract Bench_GetHistoryRow {
    data {
    n int
    }
    conditions {
    }
    action {
            var i int
            var m map
            while (i < $n) {
                m = GetHistoryRow("contracts", 1, 1)
                i = i + 1
        }
    }
}