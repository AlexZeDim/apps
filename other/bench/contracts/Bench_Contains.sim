contract Bench_Contains {
    data {
        n int
    }
    conditions {

    }
    action {
        var i int
        var s bool
        while i < $n{
            s = Contains("test", "tst")
            i = i + 1
        }
    }
}